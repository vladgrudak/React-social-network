import s from "./Dialogs.module.css"

const Dialogs = (props) => {
    return (
        <div>
            <div className={s.messages}>
                Dialogs
            </div>
        </div>
    );
}

export default Dialogs;
